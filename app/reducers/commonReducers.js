const commonReducers = {
  baseUrl: (state = {}) => state,
  serverName: (state = {}) => state,
  serverRendered: (state = {}) => state,
  locale: (state = {}) => state
};

export default commonReducers;
